<!DOCTYPE html>
<html>
<head>
  <title>Drawbridge</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= include_gon %>
  <%= javascript_include_tag "http://fallinggarden.com:9000/faye.js", "application" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <a class="brand" href="#">Conquer the Castle</a>
        <ul class = "nav">
          <% if logged_in? %>
            <li class="active">
              <%= link_to "Lobby", chat_rooms_path %>
            </li>
            <li class="">
              <%= link_to "Dashboard", dashboard_path %>
            </li>
            <li class="">
              <%= link_to "Search", search_index_path %>
            </li>          
            <!-- JQTODO: Add back in open tabs -->
            <li>
              <%= link_to "Logout", "http://localhost:3002/logout" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
  <div class="container-fluid">
    <div class="nav nav-tabs">
      <li class="active" id="lobby-tab"><%= link_to "Lobby", chat_rooms_path %></li>
      <li id="dashboard-tab"><%= link_to "Dashboard", dashboard_path %></li>
      <li id="search-tab"><%= link_to "Search", search_index_path %></li>
    </div>
  </div>
  <div class="container-fluid">
    <div class="row-fluid">
      <% if logged_in? %>
        <%= yield %>
      <% else %>
        <%= render "/shared/visitor_landing" %>
      <% end %>
    
    </div>
  </div>

</body>
</html>
