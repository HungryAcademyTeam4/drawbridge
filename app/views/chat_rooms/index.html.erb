<div class="span12">
  <h2>Castle <small>All Campaigns</small></h2>
  <% if @chat_rooms != [] %>
    <%= render partial: "chat_room_list" %>
  <% else %>
    <p>There are no chat rooms available.</p>
  <% end %>
  <%= link_to "Create a new chat room", new_chat_room_path, class: "btn" %>
</div>