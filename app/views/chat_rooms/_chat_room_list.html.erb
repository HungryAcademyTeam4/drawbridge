<table class="table span5">
  <tr>
    <td><h4>Room Title</h4></td>
    <td><h4>Enlisted</h4></td>
    <td><h4>Marching</h4></td>
    <td>&nbsp;</td>
  </tr>
  <% @chat_rooms.each do |room| %>
    <tr>
      <td><strong><%= link_to room.title, chat_room_path(room.id) %></strong></td>
      <td><%= temp_count %></td>
      <td><%= temp_count %></td>
    </tr>
  <% end %>
</table> 
